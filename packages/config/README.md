# `@lms/config`

Owns TypeScript and lint configuration that is genuinely shared across workspaces. App-specific configuration stays beside each app. The package is configuration-only and must not import application or domain code.
