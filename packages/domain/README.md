# `@lms/domain`

Future owner of pure, framework-independent domain primitives and deterministic rules that have more than one legitimate consumer. Persistence orchestration and authorization remain in the database/RLS and trusted Worker boundary. Do not create a generic utilities package here.
