# Supabase tests

This directory is reserved for database-level constraint and RLS allow/deny tests added with the first real schema issue. No domain tables or speculative policies are created in Foundation.
