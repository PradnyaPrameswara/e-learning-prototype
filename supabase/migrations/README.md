# Supabase migrations

This directory is the single authoritative, version-controlled schema migration stream. Foundation intentionally adds no product schema or placeholder migration. Future schema changes must be reviewed migration files; do not mutate a deployed schema only through the dashboard.
